import React from 'react';

const AdditionalPersonInfo = ({ personId }) => {
	const [emergencyContacts, setEmergencyContacts] = useState([]);
	const [telephones, setTelephones] = useState(null);
	const [VPI, setVPI] = setState(null);
	const [medicalInformation, setMedicalInformation] = useState(null);
	const [medicalConditions, setMedicalConditions] = useState([]);
	const [prescribedMedications, setPrescribedMedications] = useState([]);

	return <div>AdditionalPersonInfo</div>;
};

export default AdditionalPersonInfo;
